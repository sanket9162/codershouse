services:
  mongo:
    image: mongo:latest
    container_name: coderhouse-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: always

  backend:
    image: codershouse-backend:1.0.0
    container_name: codershouse-backend
    depends_on:
      - mongo

  frontend:
    image: codershouse-frontend:1.0.0
    container_name: codershouse-frontend
    depends_on:
      - backend

volumes:
  mongo-data:
